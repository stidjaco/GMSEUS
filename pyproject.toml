[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gmseus"
version = "2.0.0"
description = "GM-SEUS: Ground-Mounted Solar Energy in the United States — processing utilities"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["gmseusUtils", "osmUtils"]
